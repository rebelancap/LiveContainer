# Handoff 2026-09-03 — cert auto-import + visionOS app icon

## What happened
A user on the iloader visionOS fork could not get a signing cert into LC: "Certificate not
found. Did you log into built-in SideStore?" and the SideStore icon does nothing. Root cause:
the built-in SideStore launch path (single-app relaunch via LC2 relay → LCBootstrap
isSideStore) was never exercised on a headset. Austin's installs come from install-lc.sh with
no SideStoreApp.framework, so he only ever saw the "?" button and imported a .p12 by hand.

## Commits
- LiveContainer `visionos-native`:
  - e56d07d — `Resources/Assets.xcassets/AppIcon.solidimagestack` (2 layers). Neither the dev
    build nor the release IPA had a visionOS icon (catalog was `platform: ios` only).
  - a592167 — `LCUtils.autoImportBundledCertificateIfNeeded()`; reads
    `ALTCertificate.p12` + `ALTCertificatePassword` from the main bundle (fallback: the
    SideStoreApp.framework copy), validates via ZSign, stores like the manual import, never
    overwrites a manual import. Called first in `LiveContainerSwiftUIApp.init()`.
    Sim-verified (Lane 1) with lc-cert-modern.p12; relaunch does not re-import.
- iloader `visionos-tunnel` 84fe154 — vendored isideload `apply_special_app_behavior` also
  injects p12 + `ALTCertificateID` + `ALTCertificatePassword` into LC's MAIN bundle for
  SideStoreLc and plain LiveContainer. `cargo check` clean; no real signing pass run.

## Shipping status (2026-09-03, later the same session)
- DONE: LC release IPAs rebuilt from `build/LiveContainer-visionOS-release.xcarchive` via
  package-lc-release.sh and uploaded to rebelancap/LiveContainer tag `visionos` (both IPAs;
  icon + auto-import code verified in the archive). Rollback:
  `~/dev/vp-tools/lc-release-1.0/PREVIOUS-2026-08-08-*.bak`.
- DONE: iloader bumped to 2.2.21 (commit 64c3b5e on visionos-tunnel), built + signed:
  `~/dev/iloader/src-tauri/target/aarch64-apple-darwin/release/bundle/dmg/iloader_2.2.21_aarch64.dmg`.
  Release notes' "First run" bullet already updated to say the cert imports automatically.
- DONE: DMG notarized (Accepted, 2026-09-03), stapled, uploaded as iloader-visionOS-aarch64.dmg
  on rebelancap/iloader tag `visionos`. Both branches pushed.
- NOT DONE: on-headset check via an iloader install.

## Next steps (Austin)
1. iloader: bump version, build/notarize DMG, upload (see ~/dev/vp-tools/HANDOFF-iloader.md).
2. LC: Release visionOS archive → `~/dev/vp-tools/package-lc-release.sh` → update the
   `visionos` release assets.
3. Headset check with the shipped chain, installed via iloader (NOT install-lc.sh): open LC →
   Settings shows a certificate with no user action → install a port → Run.
4. Reply to the user; consider a plain (no-SideStore) LC IPA in the release now that the
   cert path no longer depends on SideStore.

## Open
- Built-in SideStore launch on visionOS is still untested/likely broken; now non-blocking.
- Old iloader + new LC: the framework p12 has no password key → LC logs a validation
  failure each launch and does nothing (harmless).
